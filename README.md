# 🎉 نظام حجز المناسبات - Event Booking Platform

<div dir="rtl">

منصة متكاملة لإنشاء وحجز المناسبات مبنية بـ **React 18 + TypeScript** في الواجهة و**GraphQL + Apollo Server** في الخلفية مع دعم الإشعارات الفورية عبر **Subscriptions**.

</div>

---

## 📋 جدول المحتويات

- [نظرة عامة](#-نظرة-عامة)
- [الميزات الرئيسية](#-الميزات-الرئيسية)
- [البنية التقنية](#-البنية-التقنية)
- [متطلبات التشغيل](#-متطلبات-التشغيل)
- [التثبيت والإعداد](#-التثبيت-والإعداد)
- [البنية الهيكلية للمشروع](#-البنية-الهيكلية-للمشروع)
- [واجهة برمجة التطبيقات](#-واجهة-برمجة-التطبيقات-graphql-api)
- [المكونات الرئيسية](#-المكونات-الرئيسية)
- [الأمان والمصادقة](#-الأمان-والمصادقة)
- [النشر والإنتاج](#-النشر-والإنتاج)
- [الترخيص](#-الترخيص)

---

## 🌟 نظرة عامة

<div dir="rtl">

**Event Booking Platform** هو نظام شامل لإدارة المناسبات يسمح للمستخدمين بـ:

- **إنشاء مناسبات** مع تحديد التفاصيل (العنوان، الوصف، السعر، التاريخ)
- **حجز المناسبات** التي أنشأها مستخدمون آخرون
- **إدارة الحجوزات** وإلغائها
- **البحث** عن المناسبات بالعنوان أو الوصف (server-side filtering)
- **التصفح بالصفحات** (infinite scroll pagination) لتحسين الأداء
- **تعديل وحذف** المناسبات الخاصة بالمستخدم
- **عرض ملفات المستخدمين** ومناسباتهم
- **تلقي إشعارات فورية** عند إضافة مناسبة جديدة (WebSocket subscriptions)

النظام مصمم بواجهة عربية كاملة مع دعم **RTL** وتصميم متجاوب **Responsive** يعمل على جميع الأجهزة.

</div>

---

## ✨ الميزات الرئيسية

<div dir="rtl">

### 🔐 المصادقة والأمان
- تسجيل حساب جديد مع تشفير كلمة المرور (bcrypt)
- تسجيل دخول آمن مع JWT tokens
- حماية المسارات الخاصة (Private Routes)
- تعديل الملف الشخصي (اسم المستخدم، كلمة المرور)
- حذف الحساب مع تأكيد

### 📅 إدارة المناسبات
- إنشاء مناسبة جديدة (عنوان، وصف، سعر، تاريخ)
- عرض جميع المناسبات مع عدد 8 لكل صفحة
- البحث الديناميكي بالعنوان أو الوصف (debounced search)
- تعديل وحذف المناسبات الخاصة بالمستخدم
- عرض معلومات منشئ المناسبة مع رابط لملفه

### 🎟️ نظام الحجوزات
- حجز المناسبات مع التحقق من الصلاحيات
- عرض جميع حجوزاتي
- إلغاء الحجز مع تحديث فوري للقائمة
- منع المستخدم من حجز مناسباته الشخصية

### 🔔 الإشعارات الفورية
- WebSocket subscriptions لمتابعة المناسبات الجديدة
- تحديث القائمة تلقائياً عند إضافة مناسبة
- إشعارات بصرية داخل الصفحة

### 🎨 الواجهة والتصميم
- تصميم متجاوب 100% (Mobile-first approach)
- دعم كامل للعربية واتجاه RTL
- أنماط Bootstrap مع تخصيصات CSS
- شبكة بطاقات ديناميكية:
  - 4 بطاقات في الصف (شاشات كبيرة)
  - 3 بطاقات (شاشات متوسطة)
  - 2 بطاقة (أجهزة لوحية)
  - 1 بطاقة (هواتف)
- أزرار محسّنة مع تأثيرات hover/focus
- مودالات (Modals) لإنشاء وتعديل المناسبات

### ⚡ الأداء والتحسينات
- Infinite scroll pagination (تحميل 8 مناسبات في المرة الواحدة)
- تحديثات Apollo Client optimistic
- Debounced search (300ms) لتقليل طلبات الخادم
- Lazy loading للمكونات
- Code splitting مع Vite

</div>

---

## 🛠️ البنية التقنية

<div dir="rtl">

### الواجهة الأمامية (Client)
| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **React** | 18.3.1 | مكتبة بناء واجهات المستخدم |
| **TypeScript** | 5.6 | لغة البرمجة مع دعم الأنواع |
| **Vite** | 6.4.1 | أداة البناء السريعة |
| **Apollo Client** | 3.12.4 | إدارة GraphQL والحالة |
| **React Router** | 7.1.1 | التوجيه بين الصفحات |
| **Bootstrap** | 5.3.3 | إطار عمل CSS |
| **GraphQL** | 16.10.0 | لغة الاستعلام |

### الخلفية (Server)
| التقنية | الإصدار | الوصف |
|---------|---------|-------|
| **Node.js** | - | بيئة تشغيل JavaScript |
| **Apollo Server** | 4.11.3 | خادم GraphQL |
| **Express** | 4.21.2 | إطار عمل الويب |
| **MongoDB** | 8.10.1 (Mongoose) | قاعدة البيانات |
| **JWT** | 9.0.2 | المصادقة الآمنة |
| **bcryptjs** | 2.4.3 | تشفير كلمات المرور |
| **graphql-ws** | 5.16.2 | WebSocket subscriptions |
| **TypeScript** | 5.7.3 | لغة البرمجة |

</div>

---

## 📦 متطلبات التشغيل

<div dir="rtl">

- **Node.js** 18+ و **npm** 9+
- **MongoDB** 6.0+ (محلي أو Atlas)
- متصفح حديث يدعم ES2020+
- محرر نصوص (يُفضل VS Code)

</div>

---

## 🚀 التثبيت والإعداد

<div dir="rtl">

### 1. استنساخ المشروع

```bash
git clone <repository-url>
cd web-booking-e1
```

### 2. إعداد الخادم (Server)

```bash
cd server
npm install
```

أنشئ ملف `.env` في مجلد `server`:

```env
MONGODB_URI=mongodb://localhost:27017/event-booking
JWT_SECRET=your-super-secret-key-change-this-in-production
PORT=4000
```

### 3. إعداد العميل (Client)

```bash
cd ../client
npm install
```

أنشئ ملف `.env.local` في مجلد `client`:

```env
VITE_GRAPHQL_HTTP_URL=http://localhost:4000/graphql
VITE_GRAPHQL_WS_URL=ws://localhost:4000/graphql
```

### 4. تشغيل المشروع

**تشغيل الخادم (في terminal منفصل):**

```bash
cd server
npm run dev
```

الخادم سيعمل على: `http://localhost:4000/graphql`

**تشغيل العميل (في terminal منفصل):**

```bash
cd client
npm run dev
```

الواجهة ستفتح على: `http://localhost:5173`

### 5. استخدام GraphQL Playground (اختياري)

افتح المتصفح على: `http://localhost:4000/graphql`

</div>

---

## 📁 البنية الهيكلية للمشروع

<div dir="rtl">

```
web-booking-e1/
├── client/                      # الواجهة الأمامية (React + TypeScript)
│   ├── src/
│   │   ├── components/         # المكونات المشتركة
│   │   │   ├── Alert.tsx      # مكون التنبيهات (كان Error.tsx)
│   │   │   ├── BookingItem.tsx
│   │   │   ├── EventItem.tsx
│   │   │   ├── Navbar.tsx
│   │   │   ├── ProfileEditor.tsx
│   │   │   ├── PrivateRoute.tsx
│   │   │   ├── SimpleModal.tsx
│   │   │   ├── Spinner.tsx
│   │   │   └── UserDropdown.tsx
│   │   ├── pages/             # صفحات التطبيق
│   │   │   ├── Bookings.tsx
│   │   │   ├── Events.tsx
│   │   │   ├── Login.tsx
│   │   │   ├── SignUp.tsx
│   │   │   └── UserEvents.tsx
│   │   ├── context/           # إدارة الحالة العامة
│   │   │   └── auth-context.ts
│   │   ├── graphql/           # استعلامات GraphQL
│   │   │   ├── fragments.ts
│   │   │   └── queries.ts
│   │   ├── types.ts           # أنواع TypeScript
│   │   ├── index.css          # الأنماط العامة
│   │   ├── App.tsx            # المكون الجذر
│   │   └── main.tsx           # نقطة الدخول
│   ├── index.html
│   ├── vite.config.ts
│   └── package.json
│
├── server/                     # الخلفية (GraphQL + MongoDB)
│   ├── src/
│   │   ├── config/            # إعدادات الخادم
│   │   │   └── index.ts
│   │   ├── middlewares/       # الوسيطيات
│   │   │   └── isAuth.ts
│   │   ├── models/            # نماذج MongoDB
│   │   │   ├── booking.ts
│   │   │   ├── event.ts
│   │   │   └── user.ts
│   │   ├── resolvers/         # GraphQL Resolvers
│   │   │   ├── auth.ts
│   │   │   ├── booking.ts
│   │   │   ├── event.ts
│   │   │   ├── index.ts
│   │   │   └── transform.ts
│   │   ├── schema/            # GraphQL Schema
│   │   │   └── index.ts
│   │   ├── types/             # أنواع TypeScript
│   │   │   ├── graphql-resolvers.d.ts
│   │   │   └── index.ts
│   │   └── index.ts           # نقطة دخول الخادم
│   ├── tsconfig.json
│   └── package.json
│
└── README.md                   # هذا الملف
```

</div>

---

## 🔌 واجهة برمجة التطبيقات (GraphQL API)

<div dir="rtl">

### Queries (الاستعلامات)

```graphql
# الحصول على جميع المناسبات (مع البحث والصفحات)
events(searchTerm: String, skip: Int = 0, limit: Int = 8): [Event!]!

# الحصول على مناسبات مستخدم محدد
getUserEvents(userId: ID!): [Event!]!

# الحصول على جميع حجوزاتي
bookings: [Booking!]!

# الحصول على بيانات مستخدم محدد
getUser(userId: ID!): User
```

### Mutations (التعديلات)

```graphql
# المصادقة
createUser(username: String!, email: String!, password: String!): AuthData!
login(email: String!, password: String!): AuthData!
updateUser(username: String, password: String): User!
deleteUser: Boolean!

# المناسبات
createEvent(eventInput: EventInput!): Event!
updateEvent(eventId: ID!, eventInput: EventInput!): Event!
deleteEvent(eventId: ID!): Boolean!

# الحجوزات
bookEvent(eventId: ID!): Booking!
cancelBooking(bookingId: ID!): Event!
```

### Subscriptions (الاشتراكات)

```graphql
# الاستماع للمناسبات الجديدة
eventAdded: Event!
```

### أنواع البيانات الرئيسية

```graphql
type User {
  _id: ID!
  username: String!
  email: String!
  createdEvents: [Event!]
  createdAt: String!
}

type Event {
  _id: ID!
  title: String!
  description: String!
  price: Float!
  date: String!
  creator: User!
}

type Booking {
  _id: ID!
  event: Event!
  user: User!
  createdAt: String!
}

type AuthData {
  userId: ID!
  token: String!
  username: String!
}
```

</div>

---

## 🧩 المكونات الرئيسية

<div dir="rtl">

### 1. **Events.tsx** - صفحة المناسبات الرئيسية
- عرض جميع المناسبات مع pagination (8 لكل صفحة)
- بحث ديناميكي مع debouncing (300ms)
- زر "تحميل المزيد" لتحميل الصفحة التالية
- إنشاء مناسبة جديدة عبر Modal
- حجز المناسبات
- subscriptions للتحديثات الفورية

### 2. **UserEvents.tsx** - مناسبات المستخدم
- عرض مناسبات مستخدم محدد
- إذا كان المستخدم الحالي، يظهر أزرار تعديل/حذف
- تعديل المناسبة عبر Modal
- حذف المناسبة مع تأكيد inline

### 3. **Bookings.tsx** - صفحة الحجوزات
- عرض جميع حجوزات المستخدم الحالي
- إلغاء الحجز مع refetch تلقائي
- عرض معلومات المناسبة والمنشئ

### 4. **EventItem.tsx** - بطاقة المناسبة
- تصميم متجاوب بـ Bootstrap columns
- عرض (العنوان، السعر، التاريخ، المنشئ)
- زر "التفاصيل" / "تفاصيلي" حسب الملكية
- رابط لعرض مناسبات المنشئ

### 5. **Alert.tsx** - مكون التنبيهات
- عرض رسائل (warning, danger, success, info)
- يختفي تلقائياً إذا لم تكن هناك رسالة
- يستخدم `<Alert>` من Bootstrap

### 6. **Navbar.tsx** - شريط التنقل
- روابط (المناسبات، مناسباتي، حجوزاتي)
- UserDropdown للمستخدم المسجل
- تسجيل دخول/خروج
- تصميم responsive مع collapse

### 7. **ProfileEditor.tsx** - تعديل الملف الشخصي
- Modal لتعديل اسم المستخدم وكلمة المرور
- زر حذف الحساب مع تأكيد مزدوج
- تحديث البيانات في AuthContext و localStorage

</div>

---

## 🔒 الأمان والمصادقة

<div dir="rtl">

### JWT Authentication
- يتم إصدار JWT عند تسجيل الدخول/التسجيل
- Token يُخزن في localStorage
- يُرسل في headers كل طلب GraphQL
- يتم التحقق منه في الخادم عبر middleware

### تشفير كلمات المرور
- استخدام bcrypt مع salt rounds = 12
- لا يتم تخزين كلمات المرور الأولية أبداً

### حماية المسارات
- `PrivateRoute` component يحمي الصفحات الخاصة
- يعيد التوجيه إلى `/login` إذا لم يكن هناك token

### التحقق من الصلاحيات
- لا يمكن حجز مناسبتك الشخصية
- لا يمكن تعديل/حذف مناسبة إلا لصاحبها
- لا يمكن إلغاء حجز إلا لصاحب الحجز

</div>

---

## 🚢 النشر والإنتاج

<div dir="rtl">

### بناء الواجهة للإنتاج

```bash
cd client
npm run build
```

سيتم إنشاء مجلد `dist/` يحتوي على الملفات الجاهزة للنشر.

### بناء الخادم للإنتاج

```bash
cd server
npm run build
npm start
```

### متغيرات البيئة للإنتاج

**خادم:**
```env
MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/event-booking
JWT_SECRET=<strong-random-secret>
PORT=4000
NODE_ENV=production
```

**عميل:**
```env
VITE_GRAPHQL_HTTP_URL=https://your-domain.com/graphql
VITE_GRAPHQL_WS_URL=wss://your-domain.com/graphql
```

### منصات النشر المقترحة

| المكون | المنصة | الملاحظات |
|--------|--------|-----------|
| **Client** | Vercel / Netlify | دعم Vite مدمج |
| **Server** | Railway / Render / Heroku | دعم Node.js |
| **Database** | MongoDB Atlas | مجاني حتى 512MB |

</div>

---

## 📚 السكريبتات المتاحة

<div dir="rtl">

### الواجهة (Client)

```bash
npm run dev        # تشغيل dev server (port 5173)
npm run build      # بناء للإنتاج
npm run preview    # معاينة بناء الإنتاج
npm run lint       # فحص الكود بـ ESLint
```

### الخادم (Server)

```bash
npm run dev        # تشغيل dev server مع hot reload
npm run build      # بناء TypeScript إلى JavaScript
npm start          # تشغيل الخادم المبني
```

</div>

---

## 🎯 خارطة الطريق المستقبلية

<div dir="rtl">

- [ ] إضافة صور للمناسبات (upload images)
- [ ] تصنيفات المناسبات (categories/tags)
- [ ] نظام تقييمات للمناسبات
- [ ] تعليقات على المناسبات
- [ ] إشعارات email عند الحجز
- [ ] تصدير الحجوزات إلى PDF
- [ ] دعم الدفع الإلكتروني
- [ ] خريطة لعرض موقع المناسبة
- [ ] نظام favorites/wishlist
- [ ] dashboard إداري للإحصائيات

</div>

---

## 🐛 الإبلاغ عن الأخطاء

<div dir="rtl">

إذا واجهت أي مشاكل أو أخطاء:

1. تأكد من تشغيل MongoDB
2. تأكد من صحة متغيرات البيئة (`.env`)
3. تحقق من نسخ المكتبات المثبتة
4. راجع console الخادم والمتصفح
5. افتح issue في المستودع

</div>

---

## 📄 الترخيص

<div dir="rtl">

هذا المشروع مرخص بموجب **ISC License**. راجع ملف [LICENSE](LICENSE) للمزيد من التفاصيل.

</div>

---

## 👨‍💻 المطورون

<div dir="rtl">

تم تطوير هذا المشروع لأغراض تعليمية كجزء من دورة تطوير تطبيقات الويب الحديثة.

**التقنيات المستخدمة:** React 18, TypeScript, GraphQL, Apollo, MongoDB, JWT, Bootstrap

**سنة التطوير:** 2026

</div>

---

<div align="center" dir="rtl">

**🎉 شكراً لاستخدامك Event Booking Platform! 🎉**

إذا أعجبك المشروع، لا تنسَ إعطاءه ⭐ على GitHub!

</div>
