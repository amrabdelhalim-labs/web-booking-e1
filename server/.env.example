# =============================================================================
# Server Environment Variables
# Copy this file to .env and fill in the values
# =============================================================================

# Node environment: development or production
NODE_ENV=development

# Server port (default: 4000)
PORT=4000

# MongoDB connection string
# LOCAL Development: mongodb://127.0.0.1:27017/event-booking
# MongoDB Atlas (Cloud): mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/database-name?retryWrites=true&w=majority
DB_URL=mongodb+srv://username:password@cluster.mongodb.net/event-booking?retryWrites=true&w=majority

# JWT secret key - CHANGE THIS in production!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_change_in_production

# Allowed client origin URLs for CORS (origin only, no path)
# Single origin: APP_URLS=http://localhost:5173
# Multiple origins: APP_URLS=http://localhost:5173,https://your-domain.com
# Development:
APP_URLS=http://localhost:5173
# Production example:
# APP_URLS=https://your-frontend-domain.com