# =============================================================================
# Client Environment Variables (Vite)
# Copy this file to .env and fill in the values
# All variables must be prefixed with VITE_ to be exposed to the browser
# =============================================================================

# GraphQL server HTTP endpoint
# Development:  http://localhost:4000/graphql
# Production:   https://your-api-domain.com/graphql
VITE_GRAPHQL_HTTP_URL=http://localhost:4000/graphql

# GraphQL server WebSocket endpoint (for subscriptions)
# OPTIONAL: Auto-derived from VITE_GRAPHQL_HTTP_URL if omitted
# http:// → ws://, https:// → wss://
# Only set this if you need a different WebSocket endpoint
# VITE_GRAPHQL_WS_URL=ws://localhost:4000/graphql

# Public URL where the frontend is hosted
# Development:  http://localhost:5173
# Production:   https://your-frontend-domain.com
VITE_APP_DOMAIN=http://localhost:5173

# Base path for deployment
# GitHub Pages:          /web-booking-e1/
# Vercel/Netlify/Render: /
# Local dev:             /
VITE_BASE_PATH=/
